You are extracting one instrument's ACCEPTED AMOUNT from a Bangladesh Bank
auction-result press release (titled like "Result of the Auction of Repo, ALS,
SLF, SDF and IBLF held on <date>"). The release lists, per standing-facility
instrument (Repo, SLF, SDF, ALS, IBLF), the accepted/awarded amount in Tk crore.

Indicator: {indicator_name}
Instruction: {instruction}
Value type: {value_type}
Valid range: {valid_range}

The instruction names ONE instrument (e.g. `instrument=SLF` or `instrument=Repo`).
Find the line/row/cell for THAT instrument and return its accepted amount in Tk
crore. Match the instrument label loosely (case-insensitive; "SLF" =
"Standing Lending Facility"; "SDF" = "Standing Deposit Facility"; "Repo" =
"Repurchase Agreement"). Numbers may use commas and a decimal (1,234.5 = 1234.5).
Strip commas and return a single number.

<<<HTML_TEXT>>>
{html_text}
<<<END>>>

RULES:
- If the requested instrument appears with an accepted amount, RETURN that number
  as `value` — including a genuine measured 0 (the instrument was offered/held
  but nothing was accepted). A measured 0 is a real data point: return 0, not null.
- Return value=null ONLY when the requested instrument is GENUINELY ABSENT from
  this release — e.g. Bangladesh Bank held no Repo auction that day, so there is
  no Repo line at all — OR the page is a CAPTCHA / bot-challenge / empty page.
  Do NOT invent a 0 to stand in for an absent instrument; null means "no row".
- Do not return another instrument's amount. If only SDF/SLF are present and the
  instruction asks for Repo, the answer is null.
- Reply ONLY with strict JSON of the form:

{{"value": <number-or-null>, "reason": "<1 sentence on where you found it OR why it is absent>"}}

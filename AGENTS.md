# AGENTS.md — EconDelta

Operational rules for AI coding agents (Claude Code, Cursor, Codex CLI, etc.) working in this repo. Read this in full before making any code change.

## What this project is

EconDelta is the **data layer** for Bangladesh economic indicators. ~60 series scraped daily from Bangladesh Bank, BBS, NBR, DSE, DAM, and news outlets; parsed (deterministic regex / table extractors + Claude LLM fallback); written to a canonical `data/latest.json` snapshot AND a row-per-indicator-per-day in Supabase `metric_history`. Python 3.11+, deployed via systemd timers on ExonVPS (Exonhost BDIX, Dhaka). A Mac-laptop launchd path is a belt-and-suspenders backup for two flaky scrapers. Downstream apps (The Brief, Mission Control) read from Supabase — they do NOT depend on EconDelta's Python code. The dashboard PWA at `econdelta.clauding-lab.com` (Next.js, GitHub Pages) is the human surface.

Owner: solo dev (Adnan, Bangladesh, UTC+6). Vibe-coded — Adnan directs AI agents, does not hand-write code himself. All explanations, summaries, and prose should be in **plain English with technical terms briefly explained**, never assume Adnan reads code.

## Repository structure

```
aggregate_latest.py   top-level aggregator: merges scrape outputs → data/latest.json + Supabase
fetch_all.py          top-level fetcher orchestrator (Stage 1 — pull raw artifacts)
parse_all.py          top-level parser orchestrator (Stage 2 — LLM extraction)
scrapers/             one-shot scrapers (bb_forex, bb_forex_captcha, commodity_prices, dse_market)
fetchers/             reusable fetchers — html, pdf (+ stealth Playwright variant), news article discovery
parsers/              extractors — html_*, pdf_*, hybrid, dam_ticker, dse_sector_heat, registry
claude_max/           Claude Max OAuth client + validators + prompt files
utils/                anomaly, calendar, http_client, notifier (Discord), opus_review, schema, supabase_writer
config/               sources-v3.json (indicator registry), holidays_2026.json, thresholds.json
data/                 per-indicator <date>.json + _html/ + _pdfs/ + latest.json + archive/
db/                   schema.sql (canonical reference snapshot) + README
supabase/             config.toml + migrations/ (applied via `supabase db query -f`; see db/README)
deploy/               systemd .service + .timer units + install.sh + logrotate.conf
laptop/               Mac launchd .plist files + run-and-sync.sh
pwa/                  Next.js dashboard (deploys to GitHub Pages)
scripts/              build_catalog.py and other one-off ops scripts
docs/                 data-contract, indicator-catalog (autogen), superpowers/specs|plans/
tests/                the full pytest suite (982 passed as of this PR; count grows)
```

## Build, Test, Run

| Goal | Command |
|---|---|
| Dev loop (run one scraper) | `python -m scrapers.bb_forex` |
| Dev loop (run aggregator) | `python -m aggregate_latest` |
| Unit + integration tests | `pytest` |
| Tests with coverage | `pytest --cov=utils --cov=scrapers --cov=fetchers --cov=parsers --cov=claude_max --cov-report=term-missing` |
| Lint | `ruff check .` |
| Format | `ruff format .` |
| Regenerate indicator catalog | `python scripts/build_catalog.py > docs/indicator-catalog.md` |
| VPS deploy (backend) | `ssh exonhost 'cd ~/econdelta && git pull origin main'` |
| VPS install/refresh timers | `bash deploy/install.sh` (run on VPS as sudo-capable user) |
| PWA deploy | automatic via `.github/workflows/pwa-deploy.yml` on `pwa/**` changes |

There is no backend CI — the only GH Action is `pwa-deploy.yml` and it ONLY fires when `pwa/**` or the workflow itself changes. Backend changes (`scrapers/`, `aggregate_latest.py`, `config/sources-v3.json`, etc.) DO NOT run tests in CI. Run `pytest` locally before merging anything outside `pwa/`.

## Release flow

No version tags. Releases are continuous:

- **PWA** — `git push origin main` with any change under `pwa/**` triggers `pwa-deploy.yml` which deploys to `econdelta.clauding-lab.com`. Build step rewrites `pwa/sw.js` CACHE_NAME + stamps `__BUILD_VERSION__` into `pwa/index.html`.
- **Backend** — SSH to ExonVPS, `git pull origin main`. Systemd timers pick up the new code on next fire. No restart of timers needed unless `deploy/*.service` or `deploy/*.timer` files changed (then `sudo systemctl daemon-reload` + restart the affected timer).
- **Brief consumer** (separate repo, runs on Hetzner) — pulls `data/latest.json` via 5-min cron. The data contract — Supabase `metric_history` schema + indicator IDs in `config/sources-v3.json` — is the stable interface. Internal scraper/parser shapes are implementation details.

Pre-merge smoke list for backend changes:
1. `pytest` green locally
2. `ruff check .` clean
3. If `config/sources-v3.json` changed → `python scripts/build_catalog.py > docs/indicator-catalog.md` and commit the regenerated doc

## Coding style

- **Python:** ruff (`ruff check .` + `ruff format .`). `line-length = 100`, `target-version = "py311"`, selects E/F/W/I, ignores E501. PEP-8 with type hints on signatures.
- **JS/JSX (PWA):** no auto-formatter wired up; match the existing style in `pwa/`. Plain JS, no TypeScript. JSDoc for non-trivial functions.
- **Commits:** Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, `polish:`, etc.) with optional scope like `feat(bb_forex):`. Imperative mood. **No `Co-Authored-By: Claude` lines** — attribution is disabled globally; do not re-add.
- **Files:** keep modules focused; ~400 lines typical, 800 max.

## Known landmines (read before touching these areas)

1. **claude CLI version pin (2.1.104).** CLI versions 2.1.105+ regressed non-interactive OAuth refresh — every cron-fired `parse_all.py` call returns HTTP 401 from Anthropic. ExonVPS is pinned at 2.1.104. **Do not upgrade `claude` on ExonVPS without re-verifying the cron path works on the new version.** The Brief on Hetzner is also at 2.1.104 for the same reason. Fix-of-the-fix: `CLAUDE_CODE_OAUTH_TOKEN` from `claude setup-token` is stored in `/etc/econdelta.env` (1-year token) — that bypasses the refresh dance entirely. Don't break this env var.

2. **NEVER use `ANTHROPIC_API_KEY` anywhere.** That env var triggers pay-per-call API billing. EconDelta is on the Max subscription only. Use OAuth via `.credentials.json` or `CLAUDE_CODE_OAUTH_TOKEN`. Both `claude_max/max_client.py` and any cron-callable script must respect this.

3. **BB serves image-CAPTCHA wall to flagged IPs.** `scrapers/bb_forex_captcha.py` handles the bypass via `claude -p --model claude-haiku-4-5 "<prompt> @<imagepath>"` (the `@<path>` is part of the prompt string, NOT a separate argv). Don't rewrite this as a `--file` flag — that doesn't trigger vision-mode attachment. 3-attempt loop with 60s timeout. If you raise the attempt count, also raise the per-run wall-clock budget in the systemd `econdelta-forex.service` unit.

4. **NBR FYTD canonical source is `tax_revenue` (BB PDF), not news scrapes.** The legacy `nbr_fytd_collected_tbs` and `nbr_fytd_collected_dailystar` scrapers were retired 2026-05-25 — both hit tag-listing pages whose latest article describes different fiscal-year time windows, so the cross-check flapped. Don't re-add them. If you need a second NBR corroborator, pin a specific URL or extract the time-window explicitly.

5. **`Persistent=true` footgun on systemd timers.** Changing `OnCalendar=` + `daemon-reload` + `systemctl start <timer>` causes an immediate catch-up fire if today's instance of the new schedule has already passed in local time. Tolerable but costs ~$1 of Claude calls per catch-up. To avoid: temporarily set `Persistent=false`, or reschedule during the safe window when today's slot is still in the future.

6. **Aggregate restart cap = 3.** Systemd's `Restart=on-failure` with `StartLimitBurst=3` means after 3 consecutive failures within the `StartLimitIntervalSec` window, you'll see `Start request repeated too quickly` and the timer gives up. The 14:00 retry timer is the second chance. If both fail, `latest.json` carries yesterday's data forward (graceful degradation by design — don't "fix" this). **Gotcha (fixed by fix/service-restart-limit-window, PR review 2026-08-05):** the limiter trips on the *4th* start attempt, not the 3rd — systemd only refuses a start once a unit has started *more than* burst times in the window, and that 4th start lands `StartLimitBurst * (TimeoutStartSec + RestartSec)` after the first, not one retry cycle. `StartLimitIntervalSec` must be sized for that product (2700s here), not for a single cycle — the unit shipped at 1800s for months, under which the limiter never actually tripped on a slow failure. Don't re-shrink this window to "one restart fits" math; see `tests/test_deploy_units.py` for the enforced invariant.

7. **Opus aggregate review can reject the day's data.** `utils/opus_review.py` compares proposed `data` against the last 5 days of archived `latest.json`. On reject, the run exits non-zero, fires a Discord alert at `#econdelta-alerts`, and keeps yesterday's `latest.json`. This is the safety net — DO NOT bypass it without checking what it caught. Set `ECONDELTA_SKIP_OPUS_REVIEW=1` only for emergency debugging.

8. **Aggregate auto-promotes some indicators via `BRIEF_ALIASES` and `BRIEF_CONVERSIONS` in `aggregate_latest.py`.** Adding a new scraper that should appear under a `macro_*`, `fiscal_*`, `banking_*`, or `food_*` key for The Brief requires also adding to one of those dicts. After editing → regenerate `docs/indicator-catalog.md` via `scripts/build_catalog.py`.

9. **Brief tb_* tables (`tb_macro_*`, `tb_fiscal_*`, etc.) are LEGACY — DO NOT WRITE TO THEM.** No active writer; `ingest.py` was deleted in the V6 cutover; EconDelta never wrote them. If you see code referencing those tables, it's stale and the call site needs deleting, not "fixing."

10. **Don't OCR Anthropic tokens from screenshots.** OCR confuses `0`/`O`/`o` and `l`/`I`/`1` on 140-character tokens. Always paste as TEXT.

11. **Multi-`&&` chained sudo over non-interactive SSH kills the session** (observed on Hetzner; assume the same for ExonVPS until proven otherwise). One sudo per ssh invocation works.

12. **Conductor workspaces** — if Adnan is working from `/Users/adnanrashid/conductor/workspaces/econdelta/<some-branch>`, expect: (a) `gh pr merge --squash --delete-branch` prints `fatal: 'main' is already used by worktree at ...` locally but the server-side merge succeeds (verify with `gh pr view <N> --json state,mergeCommit`); (b) Conductor's session hook denies `git reset --hard` — use `git checkout -b <new-branch> <ref>` to recreate the branch pointer at the desired ref instead.

13. **Mac laptop launchd backup MUST stay enabled.** `com.clauding-lab.econdelta.bb-forex` (06:05 BDT) and `com.clauding-lab.econdelta.dse-market` are belt-and-suspenders feeds that have covered ExonVPS gaps multiple times. Their `run-and-sync.sh` rsyncs the freshly scraped JSON onto ExonVPS, so the file mtime you see in `data/<indicator>/<date>.json` may be the Mac's sync time, not the ExonVPS scraper's write time.

14. **`/etc/econdelta.env`** (mode 640, owner `root:adnan-local`) holds `CLAUDE_CODE_OAUTH_TOKEN`, `CLAUDE_BINARY=/usr/bin/claude`, Supabase service-role key, and scraper config. **Never `cat` this file in shell output, never commit it, never paste its contents into a chat.**

15. **`docs/indicator-catalog.md` is auto-generated.** Don't hand-edit. Edit `scripts/build_catalog.py` (for derived-key entries) or `config/sources-v3.json` (for scraped indicators), then regenerate.

16. **A new parser must be wired into `parse_all.py`'s auto-import block.** Adding `parsers/<x>.py` (with its `@register("<x>")` decorator) plus a `sources-v3.json` entry is NOT enough — the decorator only runs on import, and production builds `REGISTRY` from the explicit `import parsers.<x>` list near the top of `parse_all.py`. Forget it and you get `parse_one raised … "no parser registered for '<x>'"`, the indicator silently produces 0 rows, and unit tests STILL pass (they import the parser module directly, which triggers the decorator). Guard: `tests/test_parser_registry_coverage.py` runs in a subprocess and fails if any `sources-v3.json` deterministic parser isn't registered via `parse_all`'s own imports. (2026-05-29, PR #35 — the policy-rate corridor was dark for a day.)

17. **Services that shell out to `claude` under `ProtectHome=read-only` need `~/.claude.json` in `ReadWritePaths`, not just `~/.claude/`.** The CLI writes its state file `~/.claude.json` (a sibling of the `.claude/` dir) on each run; under the read-only home that write hits `EROFS`, the parse preflight fails all 3 attempts, and `parse_all` aborts (exit 1) before parsing anything. `econdelta-parse` and `econdelta-aggregate` carry `*.service.d/10-claude-json-writable.conf` drop-ins (committed in `deploy/`) that add the carve-out. Diagnose sandboxed-service failures in `logs/<unit>-systemd.log` (the unit's `StandardError=append:` target), NOT journald — that's why the failure was invisible for 4 days. (2026-05-29 — every daily Supabase value was a stale carry-forward while parse was dead.)

18. **No Supabase read helper in Python — reads are hand-rolled GETs.** `utils/supabase_writer.py` is POST/PATCH only; `opus_review.load_history` reads LOCAL `data/archive/latest_*.json`, not Supabase. The weekly briefing job's `utils/supabase_reader.py` adds the read side (PostgREST `GET` via `requests`, mirroring the writer's `apikey`+`Bearer` auth headers, raising `SupabaseReadError`). **RLS UPDATE (verified live 2026-07-09, E3.1):** the old "daily `metric_history` has only `service_role_all` (no anon-read)" claim is SUPERSEDED — `metric_history`, `metric_history_monthly`, `auction_calendar`, and `auction_results` are ALL anon-readable (the PWA reads them with the anon key). `metric_history` and `metric_history_monthly` each carry TWO redundant anon SELECT policies (`anon_read_*` + a legacy `anon read *` duplicate) — a dedupe candidate (SQL in `docs/data-contract.md`). The briefing job and the freshness sentinel still read with the **service-role** key on-box because they need write access, not because `run_logs` is anon-read-protected — it is NOT: `run_logs` IS anon-readable (`pwa/lib/supabase-client.js` issues a direct REST `GET` on `/rest/v1/run_logs` with the anon key for the PWA's Runs page, `pwa/config.js` documents this explicitly). CORRECTION (2026-08-08, PR #120 review): this landmine previously claimed the opposite (`run_logs` "NOT anon-readable") — that was stale; `run_logs.error` being world-readable is exactly why `utils/run_log_capture.py::scrub_secrets` exists as a choke point before anything reaches that column. (2026-05-30 briefing pipeline; RLS re-verified 2026-07-09; run_logs correction 2026-08-08.)

19. **`deploy/install.sh`'s timer-enable loop is a hardcoded list, not a glob.** The unit-copy step globs `econdelta-*.service|.timer` so new files are copied automatically — but the `systemctl enable --now` loop names each timer explicitly. A new `econdelta-*.timer` gets copied but NEVER enabled unless added to that loop. `econdelta-briefing` was added 2026-05-30; the next new timer must be too. (Also the first weekly `OnCalendar=Mon …` timer — all others are daily `*-*-*`.)

20. **Two parallel metric systems — don't mix namespaces.** DAILY: `metric_history` + `metric_definitions`, ids from `config/sources-v3.json` (e.g. `call_money_rate`, `tbill_182d_yield`, `policy_rate_repo`). MONTHLY: `metric_history_monthly` + `metric_definitions_monthly`, ids from `scripts/seed_macro_monthly.py` KEY_MAP, all suffixed `_monthly`. They double-count concepts (CPI, policy rate appear in both). The weekly briefing reads the DAILY system (what YieldScope surfaces). Note: policy rates + CPI are `monthly` cadence even within the daily pipeline, so weekly anomaly diffs on them are usually flat — they fire on month-change. (2026-05-30, weekly briefing pipeline.)

21. **`claude` effort levels differ by CLI version — the briefing job needs `BRIEFING_EFFORT=max` on the box.** The pinned ExonVPS CLI (`claude 2.1.104`, see landmine 1) accepts `--effort low|medium|high|max`; `xhigh` (the briefing's code default, valid on newer CLIs) is REJECTED with `option '--effort' argument 'xhigh' is invalid`. The orchestrator reads a `BRIEFING_EFFORT` env override — set `BRIEFING_EFFORT=max` in `/etc/econdelta.env` on the box (`install.sh`'s scaffold now seeds it for fresh installs; the existing live env file must be edited by hand). Separately, whether `opus[1m]` (the briefing's default model) resolves on 2.1.104 is UNVERIFIED — if it errors set `BRIEFING_MODEL=claude-opus-4-8` (the string the 4.8 bump uses); if THAT also fails the CLI predates Opus 4.8 entirely (don't blind-upgrade — landmine 1). (2026-05-30, briefing deploy.)

22. **`upsert_metric_history(url=...)` is the Supabase base-URL OVERRIDE, not a provenance field.** `_resolve_credentials` does `resolved_url = url or os.environ["SUPABASE_URL"]`, so passing a source-page URL there silently POSTs the write to the SOURCE host. The Tier-2 scrapers `imf_eff` / `imf_debt_gdp` / `world_bank_pink_sheet` each passed their source URL → writes went to `www.imf.org` / `thedocs.worldbank.org` (a 301→2xx page logged as "upserted N rows" but persisting NOTHING, or a 404 Adobe-Helix page), while `run_logs` (no override) landed — so logs looked clean but `metric_history` stayed empty. `metric_history` has NO `url` column. `source` (NOT NULL, migration 0001) records the ORIGINATING ORGANIZATION; `provenance` (nullable, migration 0013) records the EXTRACTION METHOD — never conflate the two. NEVER pass `url=` from a scraper, and verify a write LANDED (re-query the table) rather than trusting a 2xx / "wrote N rows" log. (2026-06-01, PR #55.)

23. **IMF + World Bank hosts have IPv6 blackholed from the ExonVPS box — force IPv4 for those fetches ONLY.** `www.imf.org` and `thedocs.worldbank.org` time out on `curl -6` (~25s) but work on `-4` (~1.5s); a default dual-stack fetch hangs on the dead AAAA until systemd kills the one-shot service (`run_logs` shows `status=running`, never finished). Wrap those fetches in `utils/ipv4.force_ipv4_only()` (saves → sets urllib3 `HAS_IPV6=False` → restores in `finally`) — scope to the FETCH only; never leave the global flipped (Supabase is IPv4-only/no-AAAA so a bleed is harmless there, but it's still a latent foot-gun). New scrapers hitting other blackholed hosts should reuse this helper, not re-pin `HAS_IPV6` inline. (2026-06-01, PR #54.)

24. **BB auction RESULTS + CALENDAR come from `monetaryactivity/treasury` + `auc_calendar/1` — NOT the `/rrpt/` press release or the bare `auc_calendar`.** BB retired the per-business-day `/rrpt/` auction-result press release (now a PDF at `mediaroom/press_release/press/pr<id>_<date>.pdf` behind an F5 BIG-IP + image-CAPTCHA wall the renderer/solver CANNOT clear — 5 retrieval methods proven to fail; do not waste time re-attacking it). `scrapers/bb_auction.py` instead solver-fetches `monetaryactivity/treasury` (a 2-row grouped-header HTML table — `Bids received` colspan-3 / `Bids accepted` colspan-7, so map columns by header GROUP, landmine E) via `parse_treasury_results`, and the forward calendar from `auc_calendar/1` ("Yearly calendar" — a CSS div-grid of `div.row-header`+`div.row-data`/`div.column`, two grids bills-then-bonds in document order) via `parse_yearly_calendar`. The bare `auc_calendar` ("Yet to bid") no longer renders a `<table>`. `auction_date` (results) is the table's *Issue date* (settlement, ~1 business day after held). The scalar `slf_draw_cr`/`bb_repo_usage_cr` (OMO "Open Market Operations as on…" press release) were RETIRED in PR #62 — that release is also a walled PDF, no HTML source carries the SLF/Repo accepted amounts, and 7 retrieval methods (incl. an in-iframe solve with `claude-sonnet-4-6`) failed; deleting them removed the last `/rrpt/` consumer, so `fetchers/rrpt_discovery.py` + `parsers/html_auction_press_row.py` + the `fetch_all` `latest_rrpt_link` branch are GONE. Do not re-add a `/rrpt/` or OMO-PDF fetch path. (2026-06-02, PR #59 + #62.)

25. **PostgREST bulk-upsert (a POSTed JSON array) 400s with PGRST102 "All object keys must match" unless EVERY object carries the same keys.** Auction RESULTS rows are heterogeneous (bonds have `wam`, bills don't), so a mixed batch is rejected wholesale — even though each row is individually valid. `utils/supabase_writer._validate_auction_rows` unions the keys present across the batch and fills each missing column with `None` (real SQL NULL, never a fabricated 0). Any new row-table writer that batches heterogeneous rows must do the same; a homogeneous batch (e.g. the calendar) is unaffected. (2026-06-02, PR #60.)

26. **`source_as_of` must be recovered on the LLM-extract fallback AND propagated through `BRIEF_ALIASES`/`BRIEF_CONVERSIONS`.** The QFSAR (NPL/CAR) parses via `pdf_component`, which FAILS on the report's exec-summary prose → `hybrid.parse_one` falls to the LLM extract path. That path used to drop `source_as_of`, so a quarterly metric got stamped with TODAY's run date and a stale Q3-2025 NPL (35.73%) read as "fresh" on The Brief. Fix (PR #64): `PdfComponentParser.recover_source_as_of` runs in the LLM fallback; `_extract_quarter_end` now also matches the report's real idiom "(as of) end-<Month> YYYY" and "Month–Month YYYY" — NOT only "Quarter ending DD Month YYYY". CRUCIALLY (PR #65): the brief reads the brief-side ALIAS (`banking_npl_pct`), not the EconDelta id (`gross_npl_ratio`), so `_build_source_as_of_map` MUST propagate each recovered date to its `BRIEF_ALIASES`/`BRIEF_CONVERSIONS` target or the SPA never sees it. `_build_source_as_of_map` also WARNS when a `quarterly`/`fiscal_year` metric has no `source_as_of` — those warnings (`debt_gdp_ratio`, `gdp`, `fy_*`, debt stocks) flag the next parsers needing date recovery. `as_of` = the reporting period, NEVER the run date. This applied to the LLM-extract/PDF path only — the aggregate's OWN Tier-1 flatten path (forex/dse/commodity) forged `as_of` as the run date until PR #97 fixed it via `_build_tier1_source_as_of_map`; any NEW flatten/source path added there must register in that source_as_of map too, or it silently inherits the same forgery. (2026-06-04, PRs #64/#65; #97.)

27. **Media-screen (`media_screen/` + `scrapers/media_screen.py`) — precision is load-bearing.** Daily timer (21:30 BDT, landmine 19) screens Daily Star + TBS banking sections for fresher/conflicting BB figures → inserts into the `media_review` queue (migration `0010`) + one Discord digest; approve/reject flips status (Copotron via forced-command SSH `deploy/media-decide-ssh.sh`, or `python -m media_screen.decide approve|reject <id>` — race-safe conditional `status=eq.pending` PATCH, repeat = no-op); `aggregate_latest._apply_media_overrides` runs AFTER the normal upsert, applies `approved` rows at the press period (`source='media-approved:<outlet>'`), and supersedes once BB's pipeline reaches that period or revises it (D6). EconDelta stays the SOLE `metric_history` writer (Phase-1 screen writes ONLY `media_review`). Every `MetricSpec` needs a `valid_range` unit guard (a % ratio must reject Tk-crore amounts like 588704) AND the extraction prompt must return ONLY the overall sector-wide figure — without both, ONE NPL article yields ~13 junk candidates (amounts-as-ratios, per-segment ratios mislabelled as the sector). Section-path article patterns: Daily Star banking articles live at `/business/news/` + `/business/economy/news/`, NOT `/business/banking/`. (2026-06-04, PRs #66/#67/#68.)

28. **Media-screen daily report + the `approve N` = `media_review.id` contract + catalog alias uniqueness.** (Extends landmine 27.) `run_screen` now ALWAYS posts a report — even a 0-candidate run posts a "no change" heartbeat listing each tracked figure it saw and WHY it was skipped. `classify` is a TOTAL function `Candidate | Skip` (6 reasons as of PR #116: `out-of-range`/`no-period`/`matches-current-data`/`older-period`/`already-in-review`/`no-catalog-match` — the last one is assigned in `run_screen` before `classify` is even called, for an extraction whose label matched no catalog spec); a `Skip` must NEVER reach insert — `run_screen` routes on `isinstance(c, Candidate)` (the old `if c is not None` would insert a truthy Skip). The report posts to **`MEDIA_SCREEN_WEBHOOK_URL`** (#thebrief), which is REQUIRED: if unset, `run_screen` logs + SKIPS the post — it must NOT pass `webhook_url=None`, which would silently fall back to the ops `DISCORD_WEBHOOK_URL` and misroute the report; operational errors DO stay on `DISCORD_WEBHOOK_URL`. CRITICAL: the digest numbers each candidate by its REAL `media_review.id`, NOT the list position — `insert_media_review_rows` returns the inserted ids (`Prefer: return=representation`) and `run_screen` inserts-then-formats (`zip(ids, candidates)`). The pre-fix digest numbered by `enumerate` index and only "worked" in the first live test because ids 1,2 happened to equal positions 1,2; `approve N` would silently hit the wrong/old row on every later run. The catalog matcher is last-writer-wins (`{n.lower(): spec}`), so EVERY press alias must be UNIQUE across all specs — `tests/test_media_catalog.py::test_no_alias_collisions` guards it. Catalog is 13 metrics as of PR #69 (added repo/call-money rates, 91d T-bill yield, food inflation, FY remittance/export, NBR revenue, USD/BDT); `usd_bdt_exchange_rate` tracks the BB crawling-peg MID rate, so press segment rates (interbank/selling/kerb) usually skip. Copotron's Hetzner `~/CLAUDE.md` block + the report channel are now #thebrief. (2026-06-04, merge `6fa68b5` + PR #69 `fd71264`.)

29. **PDF `source_as_of` recovery: gate report-detection on PDF CONTENT (not URL host), and pick the LATEST idiom match (not the first).** When adding date recovery to a parser shared across sources (`pdf_table_row` serves 25 indicators / 4–5 report families), two traps bite. (a) **Host gating is wrong** — `fetch_all.py` reassigns `url` to the discovered PDF link before `fetch_pdf`, so `FetchResult.source_url` is the RESOLVED link. For MoF that's a third-party object store (`objectstorage.…oraclecloud15.com`), NOT `mof.gov.bd`; gating on the config host returns `None` for every MoF metric. Identify the report by a stable CONTENT marker — its own title in the extracted text (`"major economic indicators"` / `"quarterly debt bulletin"`, the FULL title so a sibling MoF fiscal report that merely *mentions* "debt bulletin" isn't mis-gated). (b) **First-match is a stale-date trap** — these gov reports print comparison/prior dates (`as of 30 June 2024`, `up to Jun FY25`); `.search()` first-match can lock onto the STALE one, defeating supersession (the NPL-class bug this recovery exists to prevent). Use `finditer` + take the LATEST date. Idioms confirmed against live PDFs: BB MEI `Monthly Update (April 2026)` / `Volume 04/2026 April 2026` → month-end; MoF `As of 31 December 2025` (preferred, exact day) else `up to Dec FY26` (BD fiscal: Jul–Dec = prior calendar year) → quarter-end. A malformed preferred-idiom day (`31 February`) returns `None`, never falls through to the unrelated fallback's month. Unrecognised report / no date → `None` (the slow-cadence guard catches it; never fabricate). Recovery is isolated so it can never break value extraction; mirrors `pdf_component.recover_source_as_of` for the LLM path. `recover_source_as_of` is forward-looking (dates the NEXT parse; existing rows need a re-run to backfill). (2026-06-04, PR 1 / merge `6a42a15`. PR 2 = `pdf_component` WSEI fiscal-year metrics, still open.)

30. **The `Tests` CI (`.github/workflows/test.yml`) MUST install the Playwright headless shell before `pytest` — the suite is NOT fully browserless.** Two `tests/test_html_fetcher.py` cases launch a REAL headless Chromium against a `file://` page (not mocked), so they pass locally only because a browser is already installed; a clean runner fails with `BrowserType.launch: Executable doesn't exist …chrome-headless-shell`. The job runs `python -m playwright install --with-deps --only-shell chromium` (the `--only-shell` is enough — the fetcher runs headless). ruff + pytest fire on push-to-main and every PR; the suite is otherwise secret-free (env-touching tests use fakes), so no Actions secrets are needed. Don't delete the playwright step or assume "the suite is hermetic." (2026-06-05, PR #70 / merge `c27143a`.)

31. **Library/framework API calls → Context7 first.** Before writing or editing code that calls a third-party library or framework API, query **Context7** for current, version-pinned docs — do NOT rely on training-cutoff memory.
    - **Flow:** `resolve-library-id` (name → `/org/project` ID) → `query-docs` (PIN the version this repo ships, e.g. `/pydantic/pydantic/v2`).
    - **Applies to:** pydantic v2 (`>=2.8`), playwright (`>=1.49`) + playwright-stealth (`>=2.0`), pdfplumber (`>=0.11`), beautifulsoup4 (`>=4.12`), requests (`>=2.32`), yfinance (`>=0.2.40`), pytesseract / pdf2image / Pillow.
    - **Skip for:** business/domain logic, general programming concepts, or libraries Context7 does not index.
    - **Query specifically:** library + version + exact task (e.g. `pydantic v2 model_validator mode=before for sources-v3 indicator specs`), never one-word topics like "auth".

32. **The MFR fiscal parser (`scripts/mfr_parser.py` + `scripts/backfill_fiscal.py`) only reliably handles FY26-and-forward; FY25/FY24 and older months are a hand-verified STATIC backfill, not a parse.** MoF reflows the Table-6/Table-4 columns between issues (an inserted "Revised Budget FYxx" column, a dropped prior-FY full-year column, trailing memo columns), so the value-anchor `+1/+2` offset mis-reads older reports (Apr-2025 read the FY25 revised budget 99,000 as April's single-month). Per-FY budget anchors live in `FY_BORROW_BUDGET`/`FY_NBR_BUDGET` (FY26 104000/499001 reproduce the live DB; FY25 137500/480000; FY24 132395/430000); `parse_one_mfr` raises (→ skip) for an unknown FY rather than mis-anchoring, and the FYTD self-check is a HARD GATE (`self_check_failures` drops non-reconciling months). Historical months are `STATIC_NBR_MONTHLY`/`STATIC_BORROW_MONTHLY` + `build_static_monthly_rows()`, run via `--static-only` (idempotent upsert). NBR is clean (self-check reconciles, `source='mof_mfr_static'`); **govt bank borrowing is RESTATED between issues** so it does NOT reconcile — its rows are provisional (`source='mof_mfr_static_provisional'`), and Aug-2024 is intentionally omitted. Don't try to "fix" the parser to cover old reports, and don't trust the FYTD self-check to validate borrowing. (2026-06-07, PR #72 / merge `957e68f`.)

33. **Incomplete-TLS-chain hosts (DSE) need the intermediate BUNDLED — never `verify=False`.** DSE's servers (`www.dse.com.bd`, `dsebd.org`) send only the leaf (`CN=*.dsebd.org`), omitting the `Sectigo Public Server Authentication CA DV R36` intermediate. certifi trusts the *root* (`…Root R46`) but `requests`/`urllib3` do NOT AIA-chase the missing intermediate the way browsers do, so every DSE fetch dies `CERTIFICATE_VERIFY_FAILED` (`openssl s_client -connect www.dse.com.bd:443` → `Verify return code: 21`). This froze `dse_market`/`dse_dayend` for ~24 days from 2026-06-11 after DSE's ~2026-06-09 cert renewal — an external cause, not our code. Fix: the intermediate is vendored at `fetchers/ca/sectigo_r36.pem` — the ONE canonical cert location, shared with `fetchers/tls.py`'s host-scoped urllib path (mof.gov.bd has the same disease); NEVER create a second cert dir (two copies = two rotation points = silent drift; a guard test enforces this) — and merged with certifi in `utils/ca_bundle.combined_ca_bundle`, which `HttpClient.__init__` sets as `session.verify` (ADDITIVE — never removes a certifi root; degrades to certifi-only on any build failure, so it can't break other hosts). A box-level append of that intermediate to the venv's `certifi/cacert.pem` on ExonVPS + the Mac is the immediate hotfix but is FRAGILE to a `certifi` upgrade (which overwrites `cacert.pem`) — the repo bundle is the durable path; **re-run the append if certifi is ever bumped**. If DSE changes CA, refresh `fetchers/ca/sectigo_r36.pem` from the leaf's AIA URL (`openssl s_client … | openssl x509 -noout -ext authorityInfoAccess` → the `CA Issuers` URI). NEVER set `verify=False`. (2026-07-09, E1.2.)

34. **Edition-pinned document URLs freeze silently — a "download this file" URL with an embedded document/edition id is a staleness trap.** The World Bank Pink Sheet URL (`scrapers/world_bank_pink_sheet.py:PINK_SHEET_URL`) embeds a doc-id whose trailing segment is edition-scoped (`…-005001<YEAR>`): the publisher rolls to a NEW doc-id periodically and the OLD file keeps serving — frozen at that edition's last month. Result: a daily job that fetches, parses, and upserts with exit 0 forever while the data never advances (lng/palm/wheat sat at 2025-12-31 for 5+ weeks; the run wrote the SAME `(metric_id, as_of)` row every day). "Green runs + unchanging data" means check whether the SOURCE moved, not just whether the write worked. When these metrics stall for >1 month, re-check `worldbank.org/en/research/commodity-markets` for the current `CMO-Historical-Data-Monthly.xlsx` link and bump the doc-id; the durable fix is discovering the latest link from the landing page instead of pinning (follow-up), and the freshness sentinel (E2) catches the next roll. Audit any new scraper's URL for embedded doc/edition/build ids before shipping it. (2026-07-09, E1.5.)

35. **A PDF indicator's month-dir can hold MORE than one issue — parse the newest by mtime, never `glob()[0]`.** `fetch_pdf` writes into `data/_pdfs/<id>/<CURRENT-month>/` and the discovered filename is edition-named (`2026_may.pdf`). When a source publishes a new issue mid-calendar-month, BOTH issues land in the same month-dir (ExonVPS observed: `money_multiplier/2026-06/` held `2026_april.pdf` AND `2026_may.pdf`; `2026-05/` held january+march+april). `parse_all._load_artifact_for` picked `pdfs[0]` from an UNSORTED `glob("*.pdf")`, so parse could read the STALE issue even though the fresh one was fetched — that is exactly how the pipeline "held April 2026 while BB published May 2026". The `discover_latest_pdf_link` step is HEALTHY (verified live: it picks May); the bug was purely artifact-selection. Fix (E1 leftover): `max(pdfs, key=lambda p: p.stat().st_mtime)` — discovery only ever advances to a newer issue and a cache-hit doesn't rewrite, so the freshest mtime is the latest issue. A stable single-PDF month is unaffected. (2026-07-09, E1 MEI leftover.)

36. **`config/sources-v3.json` is HAND-MAINTAINED and has DIVERGED from `config/sources-v2.json` — edit v3 directly, NEVER regenerate it.** `scripts/build_sources_v3.py` was a ONE-SHOT migration (`sources-v2.json` → `sources-v3.json`); v3 carries `"generated_from": "2.0"` but has since accumulated many hand-edits the generator has no knowledge of (the repo/sdf/slf corridor split PR #30, retirements, etc.). Re-running `build_sources_v3.py` would CLOBBER all of them — treat that script as dead/historical, not a rebuild step. To add or remove an indicator, edit `sources-v3.json` directly, then regenerate ONLY the derived `docs/indicator-catalog.md` (landmine 15). When removing indicator blocks programmatically, do it by TARGETED line-edit, NOT a `json.load`+`json.dumps` round-trip — a re-dump reformats every `anomaly_threshold` float (`0.10→0.1`) across the whole file and destroys the diff. `sources-v2.json` + the generator still carry any retired ids as harmless legacy (never re-run). (2026-07-10, PR #93 — retired 5 zero-row indicators.)

37. **On a live box, enabling ONE new timer means a targeted single-unit install — NEVER a full `deploy/install.sh` run.** `install.sh` copies every `econdelta-*.service`/`.timer` unit and `daemon-reload`s the lot; because the fleet's timers are `Persistent=true` (landmine 5), a full reinstall catch-up-fires EVERY timer whose scheduled slot has already passed today, not just the new one. Measured 2026-07-11: an install meant to enable one new timer instead off-schedule-fired `aggregate` + `briefing` + `sentinel` too, burning ~$1 of Claude calls on runs nobody asked for. To add one timer: copy just its 2 unit files (`.service` + `.timer`) into `/etc/systemd/system/`, `sudo systemctl daemon-reload`, then `sudo systemctl enable --now <new>.timer` — never touch the other units. Reserve the full `install.sh` for a from-scratch box provision or a session where every existing timer's catch-up fire is expected and accounted for.

38. **Anomaly guards must never freeze their own baseline.** A guard that rejects an anomalous value AND is the same guard that would otherwise update the baseline it compares against creates a deadlock: the first legitimate step-change looks anomalous, gets rejected, the baseline never advances past the pre-change value, so tomorrow's (now-normal) reading looks anomalous against the same stale baseline, forever. `bb_forex` reserves hit exactly this 2026-07-13→2026-08-01 — 120+ identical daily rejections against a baseline that could never move. The fix pattern (PR #96) is a month-advance gate: once the guard detects the reporting month itself has rolled forward, it persists the new value even if it would otherwise flag as anomalous, because "the underlying period changed" is a stronger signal than "the number moved a lot." Any new anomaly/coherence guard that both rejects AND gates a baseline update needs an explicit escape hatch like this, or verify by construction that rejection can never block the baseline from ever moving again.

39. **An announcement-driven rate must be read from an announcement-speed source — a monthly bulletin cannot carry an intra-month decision.** `policy_rate_repo`/`_sdf`/`_slf` were parsed from BB's *Selected Macroeconomic Indicators* monthly PDF (`publication/publictn/3/11` page 10). When the 13th MPC cut the repo rate 10.00→9.50% on 2026-07-30, the scraper kept running green and kept returning June's 10.00 — correct for that document, wrong for the world — and The Brief published a "Sub-Repo" headline that inverted sign against the real rate. Fix (PR #100): `parsers/html_labeled_value.py` reads BB's **homepage POLICY RATES panel** (`https://www.bb.org.bd/en/index.php`, `panel=div.policy label=<row>`), which moves on the announcement. Notes for anyone touching this: (a) the panel is a **div pseudo-table**, so `html_table_row` (tr/td only) cannot see it, and three panels on that page share the `display_table` class — the `panel=` CSS scope is load-bearing; (b) the parser matches labels **exactly after normalisation** and raises listing the labels it saw, on purpose — a substring match that silently locks onto the neighbouring SLF row is the failure this whole change exists to prevent; (c) **do NOT wire the panel's `Last update:` stamp into `source_as_of`** — on 2026-08-03 it read `15.02.2026` while the values were current, and the exchange-rate panel beside it was stamped the same day, so the stamp is unmaintained per-panel; (d) the page sits behind the F5/TSPD JS challenge, which `fetchers/html_fetcher.py` already clears (proven independently by `interbank_repo_data`). Generalisable: before asking "did the scraper run?", ask "could this source physically contain today's answer?" (2026-08-03, PR #100.)

40. **The stillness alarm budgets must clear several publication periods, and standing values are EXCLUDED on purpose.** `utils/staleness.check_value_staleness` (wired into `aggregate_latest`, detect-and-alert only) tracks how long each v3-registry indicator has held the same value and alerts past a cadence-derived budget — the inverse of `utils/anomaly`, which only sees failures of *motion*. Three things a future edit will want to get wrong. (a) **Budgets are deliberately loose** (`daily` 14d, `weekly` 35d, `monthly` 75d, `quarterly` 200d, `fiscal_year` 400d) because re-printing the same value between releases is NORMAL — a quarterly NPL is restamped daily at one value for ~90 days by design, and `bill_bond_rates`/`tbill_182d_yield`/`tbill_364d_yield` are nominally `daily` but really weekly auctions measured at 6 days unchanged when healthy. Tightening a budget toward "one publication period" makes the alarm fire on every healthy metric every period, which is how an alert channel gets muted. (b) **The BB corridor is exempt** (`_STANDING_VALUE_IDS`) and must stay exempt: the repo rate sat at 10.00% for six years *legitimately*, so no budget can distinguish "BB has not moved it" from "our parser is stuck". That failure mode is caught at the source instead (landmine 39) and at the consumer (The Brief ages event-cadence metrics off the restamp date). Do NOT "improve" the alarm by removing this exemption. (c) **Scope is the v3 registry only.** Replaying the rule over the other ~80 keys in the flat `data` dict (aliases, derived ratios, retired legacy ids, the writer-less `*_monthly` archive) produced **59 alerts on day one**, all restating already-known dead writers — a metric with no live writer is a missing-writer problem, not a stillness one. The tracker warms up by observation (`data/staleness_state.json`, seeded on first sight, `_MIN_RUNS_BEFORE_ALERTING`), so the already-known freezes re-surface on their own schedule rather than on the first run; never hand-seed it with backfilled dates. Measured at design time on real history: 9 alerts, all genuine — the 8 `food_*` prices (93d) plus `interbank_repo_data` (33d at 5591.93). Also found there: `debt_gdp_ratio` carries an `as_of` of **2031-12-31** in `metric_history` from a mis-parse, which is why `_parse_day` rejects future dates (a negative age would make a metric permanently un-alertable). (2026-08-03.)

41. **`bb_npl_structure` ids live OUTSIDE the pipeline config.** The 35
    banking-structure metrics (22 FSR-written, 13 seed-only press series)
    are written by scrapers/bb_npl_structure.py and
    scripts/seed_npl_structure.py. Never add these ids to
    config/sources-v3.json (each would become a daily LLM parse), never to
    briefing CORE_METRIC_IDS (owner: non-gating), and never remove them
    from sentinel ACCEPTED_STALE_METRIC_IDS (structural source lag — FSR
    is annual with ~6mo lag; band/CMSME have no scheduled source at all).
    tests/test_bb_npl_structure_wiring.py enforces all three. Note: the
    reconciliation gate wrong-column-proves only npl_rate_sector_industrial_mfg,
    npl_rate_sector_trade_commerce, npl_rate_sector_industrial_services, and
    npl_rate_sector_other; npl_rate_sector_agriculture,
    npl_rate_sector_consumer_credit, npl_rate_sector_nbfi, and
    npl_rate_sector_capital_market DO enter the weighted-average-vs-overall
    sum but each has too small a lending share to move it past the 1pp
    tolerance on a bad read — inside the reconciliation math, effectively
    unproven by it. Only npl_rate_sub_rmg, npl_rate_sub_construction,
    npl_rate_sub_housing_finance, and npl_rate_sub_smc_industries sit
    outside every reconciliation entirely and are range-only — treat their
    values with that weaker guarantee.

42. **This chain's schedule is half of a contract with The Brief, and the parse stage has a hard no-go window.** The whole chain moved to the small hours on 2026-08-04: fetch 01:10, parse 02:10, aggregate 02:55, sentinel 03:35 BDT (see `deploy/README.md` for the full table). Two forces pin it, and they pull in opposite directions. (a) **Downstream:** The Brief publishes at **08:00 BDT** reading what `aggregate_latest` wrote. Before this change the aggregate ran at 13:00 BDT against a 06:30 BDT publish — so every brief was assembled from an aggregate **~17 hours old**, and a scraper fix merged here in the morning could not reach the next morning's issue; it waited a full extra day. Nothing in either repo's test suite can see this; the two schedules were set years apart and never checked against each other. If you move a timer here, or `brief.timer` there, re-check the other side. (b) **Upstream:** `parse` shells out to the Claude CLI and must stay clear of **05:00–06:00 BDT** = 16:00–17:00 US Pacific, Anthropic's peak. The preflight failed **12 consecutive times over two days** in that window in May 2026 (commit `28bcb3d`), with exit 1 and empty stderr, while manual runs outside it passed cleanly. 02:10 BDT is 13:10 Pacific. That window — not the fetch times — is the real constraint on how late this chain may start, and therefore on how early The Brief may fire. Do not compress the chain toward dawn to "give the scrapers fresher data": Bangladesh sources publish during BD business hours, so a 01:10 fetch and a 05:00 fetch see exactly the same previous-day figures. `econdelta-npl-structure.timer` (Mon 05:30 BDT, added by PR #104) was deliberately left on its own slot — its FSR source is annual, so same-morning ordering is meaningless for it. (2026-08-04.)

43. **`ECONDELTA_PROVENANCE_ENABLED=1` before migration 0013 lands 400s every provenance-stamped write.** `utils/supabase_writer.py:upsert_metric_history` only puts a `provenance` key in the PostgREST payload when a caller passes `provenance=` AND this env flag is `1` — but the `metric_history.provenance` column does not exist until `supabase/migrations/0013_provenance.sql` is applied. PostgREST rejects an unrecognised column for the WHOLE batch (not just the offending row), so flipping the flag first would 400 every write from every provenance-stamped scraper (`bb_npl_structure`, the IMF/World Bank scrapers, `backfill_dse_dayend`, `seed_npl_structure`) — a wide, sudden outage masquerading as an unrelated Supabase error. Order matters: DDL first (owner applies 0013 by hand in the Supabase SQL editor, then confirms via the migration's VERIFICATION block), flag second (`ECONDELTA_PROVENANCE_ENABLED=1` in `/etc/econdelta.env`, then a service restart). The flag's default is OFF precisely so this repo can merge and deploy safely before the DDL exists. (2026-08-05, PR #110.)

44. **Reserves gross/BPM6 split (D5) — two namespaces, two cross-column invariants, and known duplicate-alias pairs.** `scrapers/bb_forex.py::parse_reserves` reads BOTH the Gross and BPM6 columns from BB's reserves table (`utils/schema.ForexReserves.bpm6_reserves_usd_bn`), identifying each column by HEADER TEXT, never by fixed position. Header matching is AMBIGUITY-aware, not just presence-aware (2026-08-05 review H1): BPM6 is matched first with a tolerant token (`bpm[ -]?6`, catches "BPM6"/"BPM 6"/"BPM-6"), excluded from the Gross scan, and Gross must then match in EXACTLY ONE remaining cell — zero or multiple candidates both raise `ParseError` (a stray unrelated "gross"-labelled column silently winning by being scanned last was a reproduced failure, not hypothetical). Two cross-column invariants gate the write, both raising `ParseError` (refuses the whole run, rates included) on violation: (1) `bpm6 < gross` (direction — a column swap; closes the hole landmine 38's warn-only guard couldn't), and (2) `bpm6/gross` inside `[0.70, 0.95]` (magnitude — 2026-08-05 review H2; catches a same-direction unit/decimal corruption the direction check alone cannot see. That band was calibrated against the FULL real fixture history, not just the reserves-memo's narrow recent tail — don't tighten it toward the memo's ~0.876 figure without re-checking `scripts/_seed_data/bb_reserves_gross_bpm6_history.json`, whose real observed ratio ranges 0.7643-0.8763). The existing DAILY `fx_reserve_gross_and_bpm6` / `gross_reserves_usd_bn` write (metric_history) is untouched. `aggregate_latest._write_reserves_monthly_split` writes `gross_reserves_usd_bn_monthly` + `net_reserves_bpm6_usd_bn_monthly` into the MONTHLY namespace (landmine 20) via `utils.supabase_writer.upsert_metric_history_monthly` / `upsert_metric_definitions_monthly` (additive functions mirroring `scripts/seed_macro_monthly.py`'s merge-duplicates convention — NOT the daily `upsert_metric_history`/`upsert_metric_definitions_seed`), gated on `bb_forex_ok`. `as_of` is `_month_end(reserves.reserves_date)`, matching the daily alias's own month-end choice and `scripts/seed_reserves_monthly_bpm6.py`'s — those two writers MUST move together or seeded history and live rows land ~30 days apart in the same series (2026-08-05 review H3). Gross and BPM6 are DECOUPLED (review M1): a reserves read with no BPM6 (older snapshot, or a genuine future layout without that column) still writes gross alone, with a warning explaining why BPM6 was withheld — only an actual invariant/ratio-band violation blocks BOTH series, because that signals corruption, not absence. See `docs/data-contract.md` §10.7 for the three confirmed duplicate metric_id alias pairs (never double-count them as independent confirmation), the fourth pair (`banking_reserve_money` vs `reserve_money`) that diverges on one date and needs its own investigation, and a fifth case (`general_inflation` vs `point_to_point_inflation`, found in config-conversion batch 2, landmine 49) that is the OPPOSITE shape — diverges on MOST dates and converges only on the latest — and is NOT a confirmed alias pair, just two metrics whose extraction has been inconsistently sourced, with an owner-gated choice of which column to anchor to (either option changes what "duplicate" or "independent" means for this pair — see landmine 49 and `docs/data-contract.md` §10.7). A seed script for historical BPM6 backfill (`scripts/seed_reserves_monthly_bpm6.py` + `scripts/_seed_data/bb_reserves_gross_bpm6_history.json`, 27 months 2024-04..2026-06) exists but is explicitly NOT run — needs the box's service key, deferred to the owner. (2026-08-05, reserves-memo / D5; hardened same-day per Opus review findings H1-H4/M1/M2/M4-M6/L1-L3.)

45. **BB's Balance of Payments table has a non-chronological column order and can carry more than one `<table>` on the page — select columns by header-text semantics, scoped to a single table, never by position.** `parsers/bb_bop_row.py` (written for `current_account_balance`, cab-memo-2026-08-05.md) exists because the prior parser (`html_table_row`, `col=2`) read a hardcoded position on BB's monthly BoP page (`/econdata/bop`), whose columns run prior-FY → current-FY-revised → current-FY-provisional left to right — `col=2` always read LAST year's number, and the parser had no unit-conversion step besides. `bb_bop_row` selects the column by scoring each header cell as `(fiscal_year_start, month_span)` from patterns like `"2025-26PJuly-May"` and picking the highest (ties broken by longest month-window — this is what BB's "R" vs "P" revision suffix cashes out to; the letter itself is never inspected) — never a fixed index, since BB's yearly BoP page (`/econdata/bop_yearly/1`, untested against this parser) has a different column count. **Header row, data row, and the unit label ("In million US$" / "In billion US$") must all be resolved WITHIN THE SAME `<table>` element** — an earlier version of this parser walked a flat, document-wide list of `<tr>`, and a review proved a decoy/extra table above the real one could make the selected column index come from one table while the value came from another, producing an in-range, `validate_value`-passing, but WRONG number (specifically: the real table's Jul-Apr column instead of Jul-May). Row-label matching is EXACT after normalisation (case-fold, whitespace-collapse), never substring — a renamed row (e.g. "A. Current Account Balance") must raise `ParseError`, not silently match a neighbour (mirrors `html_labeled_value.py`, landmine 39). **The regression fixture for any BB-table parser must be a real capture, never hand-built** — the synthetic fixture this parser replaced had its own `col=2` pointing at the CURRENT year, inverting the real page's semantics, which is exactly why the original bug shipped green for two months (see AGENT_LEARNINGS.md 2026-08-05 entry). (2026-08-05, PR #111.)

46. **`pdf_table_row`'s bare `row=<token>` grammar is first-match-wins substring matching, and a "shortest unique token THIS EDITION" anchor is not a fail-closed guarantee — use the quoted `row="<label>"` form for any multi-word anchor.** `_parse_instruction`'s legacy bare form is a SINGLE whitespace-free token (`instruction.split()` silently drops everything after the first space — `row=July-May of FY 26` captures only `"July-May"`), matched as a case-insensitive substring against a table row's first cell, first-match-wins in row order. The config-conversion batch-1 build (2026-08-05) initially used a bare fiscal-year-suffix token (`row=26`) for the 4 deficit-financing metrics on the assumption that a stale token merely fails closed. **That assumption was proven FALSE against the real fixture, same-day, by Opus review**: BB's deficit-financing table lists annual rows ("FY20".."FY25") ABOVE the current fiscal year's cumulative "July-May of FY 26" row — so `row=25` (proving the mechanism, one year back) matches the FY25 ANNUAL row's cell before ever reaching "July-May of FY 25", silently returning that row's full-year figure instead (44137.95 — coincidentally the EXACT wrong number the LLM path had also been returning, for the same underlying row-confusion reason). Nothing raises; a plausible-looking wrong number comes back. `row=26` would have broken the identical way the moment BB adds an "FY26" annual row (i.e. the very next edition after FY26 closes end-June 2026 — not "in ~11 months" as first assumed). **Fixed, not just documented**: `pdf_table_row.py` (and `pdf_table_latest.py`, which already had this grammar) now also accepts a quoted `row="<label>"` form matching the FULL string, immune to this collision (`"July-May of FY 26"` cannot be confused with `"FY26"` or `"July-May of FY 25"`) — the deficit-financing entries were switched to it. THIS is the form that actually fails closed: once the row's own label changes, the quoted anchor stops matching anything at all and `parse()` raises `ParseError`, falling through to the LLM path — `tests/test_pdf_table_row.py` pins both the fail-closed case and a synthetic-table regression proving the bare-token collision. Separately hardened in the same review: a `header="<exact line>"` optional guard (also `pdf_table_row.py`) verifies the table's own column-numbering line (e.g. `"1 2 3 4 = 2+3 5 6 = 4+5 7 8 9"`) is unchanged BEFORE trusting a positional `col=`, closing a silent-column-shift class distinct from the row-matching one above (BB inserting a column would otherwise shift every downstream `col=` without raising anything); and `pdf_component`'s `_extract_quarter_end` moved from unconditional idiom-priority to a REAL content gate — the first version merely tried the MEI idiom first (still false-positive-exposed to any document containing a coincidental "As of end <Month> <Year>" table header, found live on the MEI PDF's own page-5 liquidity table), and even the gated version's first draft used pdf_table_row.py's loose `"major economic indicators"` marker, which ALSO false-positive-matched real FSAR text (`tests/fixtures/fsr_fixture_text.txt` cites "Major Economic Indicators" as a data SOURCE, not its own title) — the shipped gate uses the fuller `"major economic indicators: monthly update"` title phrase instead, verified true only for the real MEI fixture and false for the real FSAR one. `pdf_table_latest.py` gained the same gated recovery (it previously never set `source_as_of` at all, silently stamping run-date and tripping `aggregate_latest.py`'s undated-metric warning every run — see `_NEVER_DATED_PARSE_STRATEGIES`) plus optional `page=` scoping (a row label can coincidentally also appear in the SAME page's narrative prose restating the table in a sentence — page-scoping narrows the search space, though it does not eliminate a same-page collision on its own) and a `min=` calibration rule in its own docstring (set roughly half the row's smallest LEVEL value, re-derive per row — never copy another row's threshold, since flow/pct-change columns don't scale with the level 1:1). (2026-08-05, config-conversion batch 1; hardened same-day per Opus review findings H1-H4/M1-M4/L1-L2.)

47. **A parse strategy that never recovers `source_as_of` forges `metric_history.as_of` to the run date — and neither the allow-list NOR the metric's most recent parser is the only place this bites.** `policy_rate_repo` has never had a real `source_as_of`: `parsers/pdf_table_column_latest.py` fed it from BB's Selected Macroeconomic Indicators monthly bulletin for roughly the first 60 of the 62 nights media_screen has run (2026-06-04 → ~2026-08-02) and never emits `source_as_of` at all, then `parsers/html_labeled_value.py` (landmine 39, PR #100, 2026-08-03) took over and *also* never emits it — a deliberate choice this time (its own docstring: BB's homepage panel's "Last update" stamp is unmaintained and would date a *current* value five months stale if trusted). **Fixing the attribution to only the newer parser is wrong — the forgery predates PR #100 by two months.** Neither parser is in `aggregate_latest.py`'s `_NEVER_DATED_PARSE_STRATEGIES` frozenset (`html_table_row`, `html_call_money`, `dse_sector_heat` only) — but **membership in that allow-list is NOT a fix and must never be treated as one**: it only silences the "undated metric" warning at `_build_source_as_of_map` (`aggregate_latest.py` around line 622); it does nothing to `upsert_metric_history`'s `as_of=now.date()` fallback, which fires identically whether or not the strategy is on the list. The ONLY two real fixes are (a) implement genuine `source_as_of` recovery for the parser, or (b) teach every downstream consumer of `as_of` to treat a same-day-forged stamp as "unknown, not current" — and both are OWNER decisions, because they change the data-currency semantics The Brief, the staleness sentinel, and `media_screen` all currently trust `as_of` for. Confirmed consumer: `scrapers/media_screen.py::_parsed_for` reads this forged `as_of` as `parsed_as_of`, and `media_screen/filter.py::classify()`'s Rule 2 compares a press article's stated (necessarily past) date against it — landing on `Skip("older-period")` on nearly every input, permanently masking real fresher-period candidates for `policy_rate_repo` and `call_money_rate` (which IS on the allow-list — an accepted, equally-exposed gap) alike. This is HOW `bb-cuts-policy-rate-95-after-2-years` (BB's real 2026-07-30 cut) never once became a media-screen candidate across 62 consecutive nights (AGENT_LEARNINGS.md 2026-08-05, "The media screen ran green and empty"). Separately, the comment above `_NEVER_DATED_PARSE_STRATEGIES` (`aggregate_latest.py` ~lines 65-67) claiming "every PDF parser DOES already attempt source_as_of recovery" is FALSE — `pdf_table_column_latest`, `pdf_mfr_row`, and `pdf_table_total` (verified: no `source_as_of` anywhere in any of the three) recover none; corrected in this PR. **Not fixed as of this entry** — see (a)/(b) above; this landmine documents the mechanism for the sign-off decision, not a shipped fix. Before trusting `metric_history.as_of` for ANY new freshness-sensitive comparison, check what the metric's CURRENT parser actually recovers — not what the allow-list says, and not what history says a prior parser did. (2026-08-05, PR #116.)

48. **`SuccessExitStatus=` and `RestartPreventExitStatus=` do different jobs — only the former keeps a unit out of `systemctl --failed`.** Both accept a list of "this exit code isn't a real failure" codes, and it's easy to reach for either one interchangeably. They aren't interchangeable: `RestartPreventExitStatus=<N>` only suppresses systemd's automatic restart for that exit code — the unit STILL ends in `failed` state (ActiveState), still shows in `systemctl --failed`, still counts as a `Result=exit-code` failure. `SuccessExitStatus=<N>` is the one that changes systemd's verdict — it makes that exit code a CLEAN termination (like exit 0), so the unit ends `inactive`, does not appear in `--failed`, and (as a side effect) `Restart=on-failure` never fires for it either, because there's no failure to restart on. Every scraper with a documented, non-error non-zero exit currently uses `RestartPreventExitStatus=`, not `SuccessExitStatus=` — `bb_forex.service` / `briefing.service` for exit 2 ("anomaly hold" / "data stale", a gate skip that's worth a distinct "ran, but held/skipped a write" signal, not full green), and (as of this PR) `econdelta-dse.service` / `econdelta-commodity.service` for the same exit-2 "anomaly, write skipped" pattern in `dse_market.py`/`commodity_prices.py` — those last two had NO exit-2 handling at all until this PR (not a considered fleet-wide choice; they were simply missed when the pattern was first introduced), so a deterministic anomaly could retry-storm exactly like `econdelta-npl-structure.service`'s exit-3 skip did (4 starts inside the `StartLimit` window, duplicate Discord alerts, then failed) before this fix. `econdelta-npl-structure.service`'s exit-3 "already captured" skip is the one case that needed `SuccessExitStatus=3` instead, because a by-design no-op polluting `systemctl --failed` — the exact signal used to verify box health — was the defect being fixed there; the fleet's exit-2 units deliberately still show `failed` (no retry, but visibly flagged) pending an owner-level call on whether "stale" should read green instead (tracked as a follow-up, not decided here). If a future unit's documented non-zero exit should read as fully clean (not just "don't retry"), reach for `SuccessExitStatus=`; if it should stop retrying but still visibly flag for a human, `RestartPreventExitStatus=` is correct. Don't "fix" one by reaching for the other without checking which behavior the incident actually needs, and don't assume an existing `RestartPreventExitStatus=<N>` on one unit means every unit sharing that exit code has been handled — verify each one. (2026-08-05, deploy-hygiene PR.)

49. **Config-conversion batch 2 (issue #113): `bop_summary` unlocked by `bb_bop_row` (landmine 45); everything else in the 19-item disposition list re-checked LIVE and confirmed still blocked, plus three new findings.** `bop_summary`'s deterministic path was dead the same way `current_account_balance`'s was pre-PR#111 (`html_table_row` `task="Entire Table"` has no `row=`/`col=` tokens, always `ParseError`s). Pointed it at `bb_bop_row` with `row=Overall Balance` — a different row of the SAME table `current_account_balance` already reads, so no new parser needed. Parity: production held `4.019` for **12** consecutive days (2026-07-25..2026-08-05, independently confirmed against `metric_history` — extends the reviewer's own 10-day, 2026-07-27..2026-08-05 check) via the LLM path; the deterministic value is `4.019` exactly, and independently reconciles against the fixture's own BoP identity (`Current Account -301 + Capital 366 + Financial 4161 + Errors&Omissions -207 = 4019`) — see `tests/test_config_conversion_batch2.py`. No published-figure change. The disposition list has 19 rows, not 18: 17 of them are re-examined batch-1 deferrals (of the original 27), and **2 — `treasury_bill_outstanding`/`treasury_bond_outstanding` — were NEVER part of the 27-item "always LLM-only" set** (both already carry valid `row=`/`col=` grammar; their blocker is a dead source, a different failure class entirely, see finding (c) below). Re-examining the 17 batch-1 deferrals against tonight's merges (#110 provenance, #111 `bb_bop_row`, #112 reserves split): **none were unblocked** — none of those three PRs built a dynamic-month-row selector, a two-column-subtract parser, inline unit-scaling, a grouped-header row-scanner, a div-grid parser, or a date-keyed-latest-row selector, which is what `food_inflation`/`non_food_inflation`/`monthly_import_lc_opening`/`monthly_import_lc_settlement` (dynamic row), `excess_liquid_asset_total_minimum` (two-column subtract), `categorywise_export`/`categorywise_fy_import_breakdown`/`remittance_by_country` (unit scaling), `bill_bond_rates`/`tbill_182d_yield`/`tbill_364d_yield`/`tbond_5y_yield`/`tbond_10y_yield` (tenor label is the row's 4th cell, not the 1st — grouped header), `gsec_auction` (page is now a CSS div-grid, zero `<table>` elements — reconfirmed live 2026-08-05), and `interbank_repo_data` (rows keyed by auction date, no static row label — reconfirmed live 2026-08-05, current top row `29/06/2026 ... 5591.93` matches landmine 40's own reference) each still need. Three findings worth a maintainer's attention even though none were converted: (a) **`general_inflation` vs `point_to_point_inflation` is NOT a clean duplicate pair, and which column to anchor `general_inflation` to is an OWNER decision, not an engineering one** — see `docs/data-contract.md` §10.7 and `AGENT_LEARNINGS.md` (2026-08-05 entry) for the column-family-confusion evidence and the two-option tradeoff (anchor to Point-to-Point General, which duplicates `point_to_point_inflation` and breaks `general_inflation`'s own series continuity; or anchor to Twelve-month-average General, a distinct real BB concept that stays continuous with history) — plus a third same-window candidate (the Wage Rate Index table's own "Point to Point → General" column) that must be ruled out either way. Do not convert `general_inflation` until BOTH the dynamic-row blocker above is solved AND the owner picks an anchor. (b) **`non_tax_revenue` is closer to unlockable than landmine-batch-1 characterized, but still needs an owner call, not a fetch-only patch.** Its fetch bug (`fetch.task="Page 9 of doc, table 4"` against an HTML landing page) IS fixable with a one-line `"discover": "latest_pdf_link"` — verified live: `fetchers/pdf_discovery.discover_latest_pdf` resolves a real PDF from this exact URL, and the target PDF is already a committed fixture (`tests/_pdfs/c513580c-f220-49aa-9305-605585b84180.pdf`). But the report is an MFR (Monthly Fiscal Report) — the SAME report family landmine 32 built `pdf_mfr_row`/`scripts/mfr_parser.py` for BECAUSE its column grid reflows between issues and needs a per-FY budget `anchor=` to disambiguate, not a fixed `col=`. `parsers/pdf_mfr_row.py` is already registered and already has a live sibling config entry (`non_nbr_tax_revenue`, currently inert — its own `anchor=TODO_VPS_FILL_FY26_NON_NBR_BUDGET_CRORE` is an unfilled placeholder, zero rows ever written). The real Table 4 row is `"c. Non-tax Revenue"`, FY26 budget anchor `45,999` crore (cross-verified against the same PDF's Appendix 5: `45,999.4`) — but the row label also appears 3 more times in the SAME page's narrative prose (`"non-tax revenue collection"`, `"NBR Non-NBR Non-tax"` chart legend) after the real data row, so it currently only resolves correctly because the data row happens to come FIRST in document order (the exact `pdf_table_row` H1/M2-class fragility landmine 46 just hardened, not yet hardened here), and it is genuinely unclear whether the metric wants `col=month` (3,280 crore, October alone) or `col=fytd` (29,193 crore, July-October cumulative) — the config has no `cumulative: true` flag either way and zero production history exists to infer from (fetch has failed outright since inception). Do not ship a guess on either axis. (c) **`treasury_bill_outstanding` and `treasury_bond_outstanding` are SOURCE-DEAD, not parser-blocked, and were never in the original 27** — `gsom.bb.org.bd/mtm-bill.php` and `gsom.bb.org.bd/mtm.php` both return a live HTTP 404 `"File not found."` (verified live 2026-08-05, matching box-log evidence from the same night); both configs already have valid `row=`/`col=` grammar, so once BB republishes these pages (or a replacement URL is found), no parser work is needed — just a source-URL hunt. (2026-08-05, config-conversion batch 2, issue #113; Opus review APPROVE-WITH-FIXES applied same-day.)

50. **`metric_history_monthly` chart-series ids use day-1-of-data-month `as_of`; their live appender is append-only; the daily monthly_remittance/monthly_export flashes are NEVER a source for monthly finals.** 5 chart-feeding ids (`remittance_usd_mn_monthly`, `exports_usd_mn_monthly`, and the CPI trio `cpi_12m_avg_monthly`/`cpi_p2p_food_monthly`/`cpi_p2p_nonfood_monthly`) were seeded ONCE from a dead third-party site (`scripts/seed_macro_monthly.py`, source `macro_observer_seed`) and froze at `as_of=2026-03-01` for 5 months — The Brief's charts and the EconDelta PWA's /macro tab both read this table, so a frozen chart was visible to readers the whole time, hidden inside a 41-item freshness digest (2026-08-08 incident, AGENT_LEARNINGS.md). Three rules from the fix (PR "fix(monthly): live appender + official backfill for frozen chart series"): (a) **every row in these series uses `as_of` = the FIRST day of the data month** (`2026-04-01` = April data) — this matches every pre-existing seeded row (`scripts/seed_macro_monthly.py`'s `normalise_as_of`) and both new writers (`scripts/backfill_monthly_chart_series.py`, `aggregate_latest._write_macro_monthly_append`); using month-END or the run date instead forks a shadow series 30 days apart from the real one (same class of bug as landmine 44's H3 reserves-split warning). (b) `_write_macro_monthly_append` is STRICTLY APPEND-ONLY — it checks `metric_history_monthly` for an existing `(metric_id, as_of)` row before every write and skips if present. This is load-bearing, not a style choice: `scripts/backfill_monthly_chart_series.py` hand-verified official values for Apr-Jun 2026 that PATCH two known-bad daily CPI cells (`non_food_inflation` April, `general_inflation` June — see the equality guard below), and the live appender must never overwrite them with a re-derived daily value on a later run. (c) EconDelta's DAILY `monthly_remittance`/`monthly_export` ids are PROVISIONAL FLASH figures frozen at 2dp USD bn — adversarially proven unsafe to derive monthly-final chart values from (see AGENT_LEARNINGS.md). The live appender instead derives the CPI trio from the daily CPI ids (`general_inflation`/`food_inflation`/`non_food_inflation`, which DO carry a trustworthy month-end `source_as_of` — Tier-1/v3 `source_as_of` recovery, PR #97 class) and fetches remittance LIVE from BB's own official monthly wage-remittance webpage table (`https://www.bb.org.bd/en/index.php/econdata/wageremitance`, FY-row-grouped, behind the same TSPD/F5 JS challenge as landmine 39 — cleared via `fetchers/html_fetcher.fetch_html`, never a plain `requests.get`). A `general_inflation == point_to_point_inflation` equality guard (landmine 49's wrong-column defect, reproduced live for June 2026) skips the `cpi_12m_avg_monthly` write for that month rather than writing a duplicate under the wrong column family. `exports_usd_mn_monthly`/`imports_usd_mn_monthly` have NO live writer (EPB portal JS-rendered/unscrapeable; BB cif imports ~2mo lag) and sit in `sentinel.ACCEPTED_STALE_METRIC_IDS` alongside `cpi_12m_food_monthly`/`cpi_12m_nonfood_monthly` (no live source anywhere post-seed-death). `sentinel.CHART_FEEDING_METRIC_IDS` (16 ids) marks every `metric_history_monthly` id a reader-visible chart actually renders — `sentinel/report.py`'s digest lists breaches in this set FIRST under their own heading so this class of freeze can never again hide inside a wall of internal-parity breaches.

51. **The 8-tenor yield-ladder `metric_history_monthly` ids are ALL-OR-NOTHING per month, on top of (not instead of) landmine 50's day-1/append-only rules — a "live-but-unpromoted" source class, distinct from landmine 50's "dead source" class.** Phase 2 of the frozen-charts incident: `tbill_91d_yield_monthly`/`tbill_182d_yield_monthly`/`tbill_364d_yield_monthly`/`yield_2y_monthly`/`yield_5y_monthly`/`yield_10y_monthly`/`yield_15y_monthly`/`yield_20y_monthly` froze at `as_of=2026-04-01` for a DIFFERENT reason than landmine 50's five ids — `scrapers/bb_auction.py` had been capturing ALL 8 tenors into `auction_results` (a structured row table, PK `(auction_date, tenor)`) daily since May 2026 the whole time; nothing ever read that table and promoted it into the monthly namespace. `scripts/backfill_yield_ladder_monthly.py` fills the May-July 2026 gap (24 controller-computed rows, 8 tenors × 3 months); `aggregate_latest._write_yield_ladder_monthly_append` is the ongoing writer, a SIBLING function to `_write_macro_monthly_append` (not a leg folded into it) with its own call site / try-except / notify message, so a yield-ladder failure can never block the CPI/remittance legs' upsert or vice versa. **Derivation rule:** `monthly[tenor] = cutoff` of the LATEST `auction_results` row with `auction_date <= ` the target month's last day — carry-forward across months is EXPECTED (T-bill/T-bond tenors auction roughly monthly-to-quarterly, not every calendar month), fetched via `utils.supabase_reader.get_auction_results_through` (newest-first, so the first row per tenor is the latest). **ALL-OR-NOTHING is the load-bearing rule specific to this ladder** (source: `docs/data-contract.md` doesn't yet document this, but The Brief's ladder-chart contract does): the chart takes the UNION of all 8 tenors' `as_of` dates and renders the last TWO with `spanGaps` — writing a new month for SOME tenors but not others would make the chart fabricate a curve shape no single auction day ever actually quoted. If ANY tenor has genuinely NO `auction_results` row at all on or before the target month-end (not even a carried-forward one), `_yield_ladder_rows_for_month` writes NOTHING for ANY tenor that month and the caller notifies naming every failing tenor in one batched message — "no row at all" means the source table itself is broken, a structurally different failure than "the tenor just hasn't auctioned this month" (which carry-forward already handles correctly). Append-only is a SEPARATE, second-stage check on top of this: even when all 8 tenors derive successfully, a tenor that already has the target month's row is dropped from the write, never re-derived-and-overwritten. Range check `0 < v < 25` (a different ceiling than landmine 50's CPI `[0, 30)` band — yields and CPI prints occupy different plausible ranges) treats an out-of-range cutoff the same as a missing row for the all-or-nothing rule. The appender targets only the most recently COMPLETED month (`_previous_month_start`, reused from landmine 50's remittance leg) and short-circuits before reading `auction_results` at all once that month is fully written — most days in a given month are a no-op read against `metric_history_monthly` only.

## Communication & timezone

- **All times in BDT (UTC+6).** When generating timestamps, dates, or schedules, convert to BDT and label it. UTC appears in some systemd unit files and `scraped_at` ISO strings — convert before showing to Adnan.
- **Plain-English explanations** of technical terms in conversation, even obvious ones. Adnan reads but doesn't write code.
- **No emojis** in code or commits unless explicitly requested.
- **Short, scannable updates** — Adnan reads on mobile often.

## Out-of-scope behaviors

Do not, without explicit user sign-off:

- Modify `/etc/econdelta.env` on ExonVPS (or print its contents).
- Add dependencies in `requirements.txt`, `pyproject.toml`, or `pwa/package.json`.
- Edit `deploy/*.service` or `deploy/*.timer` (changes need VPS sudo + daemon-reload + careful reschedule timing — see landmine 5).
- Edit `.github/workflows/pwa-deploy.yml`.
- Edit `db/schema.sql` or anything under `supabase/migrations/` (changes hit the live, shared Supabase project). Migrations are applied with `supabase db query --linked -f supabase/migrations/<file>.sql` from a linked Mac checkout — **NOT `supabase db push`**, which fails because the DB is shared with The Brief (whose migrations aren't in this repo). Keep every migration idempotent. Never re-introduce `db/migrations/`.
- Bulk-remove indicators from `config/sources-v3.json` (single retirements with rationale are fine).
- Disable `ECONDELTA_SKIP_OPUS_REVIEW` gating in `aggregate_latest.py`.
- Run `git push --force` against any branch.
- Skip hooks (`--no-verify`, `--no-gpg-sign`, etc.).
- Push tags (no tag-driven releases here; tag pushes have no consumer but signal intent that doesn't exist).

For everything else, see `VISION.md` for what auto-merges vs needs sign-off.

## Cross-cutting rules

Adnan's global rules live in `~/.claude/CLAUDE.md` (loaded automatically by Claude Code). When that file conflicts with this one, this file wins because it's project-specific.

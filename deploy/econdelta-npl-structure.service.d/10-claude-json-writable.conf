[Service]
# bb_npl_structure calls the claude CLI via run_max (guarded FSR Table 2.3
# extraction, up to 2x 900s attempts). It writes ~/.claude.json each run;
# ProtectHome=read-only blocks it (EROFS) unless this is writable too, alongside
# the ~/.claude dir already in ReadWritePaths — see May-2026 parse-401 precedent.
ReadWritePaths=/home/adnan-local/.claude.json
